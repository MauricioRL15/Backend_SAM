



















/*import { PrismaClient, tb_roles } from '@prisma/client'

const prisma = new PrismaClient()


async function main() {
   const tb_roles = await prisma.tb_roles.create({
    data: {
     id:    1, 
     tipo: 'Administrador',
    },
   })

  }
  
  main()
    .then(async () => {
      await prisma.$disconnect()
    })
    .catch(async (e) => {
      console.error(e)
      await prisma.$disconnect()
      process.exit(1)

})


/*export const Roles = () => { }

function Editar() {
    
}

function Eliminar() {
    
}

function Actualizar() {
    
}*/