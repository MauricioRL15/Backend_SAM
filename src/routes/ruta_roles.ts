














/*import {Roles} from '../controllers/Controller_Roles';

import { Router } from 'express';

export const RUTA = Router()

RUTA.post('/conectar', Roles)
RUTA.get('/conectar',  Roles)
RUTA.delete('/conectar',  Roles)
RUTA.put('/conectar',  Roles)*/
